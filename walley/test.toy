(define x 12)
(if (== x 12) (print x)(print x))
(= add 
	(lambda (a b)
		(stms 
			(+ a b)
		)
	)
)


(= ^ (lambda (a b) (^ a (- b 1))))    

(define ^ 
	(lambda (a b) 
		(cond ((= b 0)
				())  )
		(^ a (- b 1))
	)
)    

